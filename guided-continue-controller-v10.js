'use strict';
(()=>{
  const VERSION=10;
  if((window.__guidedContinueControllerV||0)>=VERSION)return;
  window.__guidedContinueControllerV=VERSION;

  const n=v=>Number(v)||0;
  const LABELS={f_address:'Property Address',f_price:'Acquisition Price',f_units:'Number of Units',f_hold:'Expected Holding Period',f_rent:'Monthly Rent'};
  const PERCENT_FIELDS=['rentGrowth','vacancy','opEx','mortRate','appreciation','sellCost','ordinaryTax','depTax','capGainsTax','requiredReturn','desiredCap'];
  const NUMBER_FIELDS=['price','land','units','rent','depLife','hold','mortgage','loanYears','points','origFee','desiredGrm'];
  let calculating=false;

  function syncVisible(){const root=document.getElementById('gwBody');if(!root)return;root.querySelectorAll('[data-src]').forEach(input=>{const src=document.getElementById(input.dataset.src);if(!src)return;src.value=input.value;src.dispatchEvent(new Event('input',{bubbles:true}));src.dispatchEvent(new Event('change',{bubbles:true}));});}
  function currentStep(){const active=document.querySelector('#gwSteps .gw-step.active[data-step]');return active?Number(active.dataset.step):0;}
  function ensureStyles(){if(document.getElementById('guidedValidationStyles'))return;const st=document.createElement('style');st.id='guidedValidationStyles';st.textContent=`.gw-validation{display:none;margin:0 24px 12px;padding:10px 12px;border:1px solid #f3c7c7;border-left:4px solid #c43d3d;border-radius:8px;background:#fff6f6;color:#7a2525;font-size:10px;line-height:1.45}.gw-validation.show{display:block}.gw-validation b{display:block;font-size:10.5px;margin-bottom:3px}.gw-validation ul{margin:4px 0 0 17px;padding:0}.gw-validation li{margin:2px 0}.gw-field.gw-missing>label{color:#a12d2d}.gw-field.gw-missing input,.gw-field.gw-missing select{border-color:#cf5a5a!important;box-shadow:0 0 0 3px rgba(207,90,90,.12)!important;background:#fffafa}@media(max-width:650px){.gw-validation{margin-left:16px;margin-right:16px}}`;document.head.appendChild(st);}
  function ensureBox(){ensureStyles();const actions=document.querySelector('#guidedSetup .gw-actions');if(!actions)return null;let box=document.getElementById('gwValidation');if(!box){box=document.createElement('div');box.id='gwValidation';box.className='gw-validation';box.setAttribute('role','alert');box.setAttribute('aria-live','polite');actions.insertAdjacentElement('beforebegin',box);}return box;}
  function clearValidation(){const box=document.getElementById('gwValidation');if(box){box.classList.remove('show');box.innerHTML='';}document.querySelectorAll('#gwBody .gw-field.gw-missing').forEach(f=>f.classList.remove('gw-missing'));document.querySelectorAll('#gwBody [aria-invalid="true"]').forEach(el=>el.removeAttribute('aria-invalid'));}
  function showMissing(ids){clearValidation();const box=ensureBox();if(!box)return;ids.forEach(id=>{const visible=document.querySelector(`#gwBody [data-src="${id}"]`);visible?.setAttribute('aria-invalid','true');visible?.closest('.gw-field')?.classList.add('gw-missing');});const labels=ids.map(id=>LABELS[id]||id);box.innerHTML=`<b>Complete the required fields before continuing.</b><div>Please enter:</div><ul>${labels.map(x=>`<li>${x}</li>`).join('')}</ul>`;box.classList.add('show');const first=document.querySelector(`#gwBody [data-src="${ids[0]}"]`);first?.focus();first?.scrollIntoView({behavior:'smooth',block:'center'});}
  function missingForStep(i){const missing=[];const address=document.getElementById('f_address')?.value?.trim()||'';const price=n(document.getElementById('f_price')?.value);const units=n(document.getElementById('f_units')?.value);const hold=n(document.getElementById('f_hold')?.value);const rent=n(document.getElementById('f_rent')?.value);if(i===0){if(!address)missing.push('f_address');if(price<=0)missing.push('f_price');if(units<=0)missing.push('f_units');if(hold<=0)missing.push('f_hold');}if(i===1&&rent<=0)missing.push('f_rent');return missing;}
  function valid(i){const missing=missingForStep(i);if(missing.length){showMissing(missing);return false;}clearValidation();return true;}
  function safeReadState(){if(typeof state!=='object'||!state)throw new Error('Analysis state is unavailable.');for(const k of NUMBER_FIELDS){const el=document.getElementById('f_'+k);if(el&&el.value!=='')state[k]=Number(el.value);}for(const k of PERCENT_FIELDS){const el=document.getElementById('f_'+k);if(el&&el.value!=='')state[k]=Number(el.value)/100;}const io=document.getElementById('f_interestOnly');if(io)state.interestOnly=String(io.value)==='true';const addr=document.getElementById('f_address');if(addr)state.address=addr.value.trim();const name=document.getElementById('f_name');if(name)state.name=name.value.trim();const repairs=document.getElementById('f_initialRepairs');if(repairs)state.initialRepairs=Math.max(0,Number(repairs.value)||0);state.hold=Math.min(40,Math.max(1,Math.round(Number(state.hold)||1)));state.loanYears=Math.min(40,Math.max(1,Math.round(Number(state.loanYears)||30)));state.units=Math.max(1,Math.round(Number(state.units)||1));state.land=Math.min(Math.max(0,Number(state.land)||0),Math.max(0,Number(state.price)||0));}
  async function safeRecalculate(){safeReadState();const bridge=window.PropertyThesisIncomeEngineBridge;if(!bridge?.requestServer)throw new Error('Calculation service is not ready. Please refresh the page and try again.');const server=await bridge.requestServer({...state},{refresh:false});if(!server?.years?.length){const status=bridge.status?.();throw new Error(status?.lastError||'The calculation service could not complete this analysis. Please try again.');}if(typeof analyze!=='function')throw new Error('Analysis engine is unavailable.');result=analyze(state);if(!result||!result.years?.length)throw new Error('The analysis could not be calculated from the current inputs.');return result;}
  function refreshReview(){const modules=['Stage15Layout','ReviewValuation','ReviewDecisionSummary','ReviewReconciliation','ReviewCashflowStatement','ReviewTaxesOperations','ReviewTaxesSale','ReviewCleanup','ReviewFinancingSummary','ReviewTotalInvestmentCashflow'];modules.forEach(name=>{try{window[name]?.apply?.();}catch(e){}});try{window.InitialRepairsModel?.enhanceResults?.();}catch(e){}}
  function activateDashboard(){const target=document.getElementById('dashboard');if(!target)throw new Error('Review Results section is unavailable.');try{window.PropertyThesisIncomeEngineBridge?.browserRender?.();}catch(_e){}document.querySelectorAll('.section').forEach(sec=>sec.classList.toggle('active',sec===target));document.querySelectorAll('.nav [data-tab]').forEach(btn=>btn.classList.toggle('active',btn.dataset.tab==='dashboard'));document.querySelectorAll('#stage8Workflow [data-s8-tab]').forEach(btn=>btn.classList.toggle('active',btn.dataset.s8Tab==='dashboard'));try{window.Stage8Workflow?.refresh?.();}catch(e){}try{window.Stage10Workflow?.refresh?.();}catch(e){}try{window.Stage15Layout?.apply?.();}catch(e){}setTimeout(refreshReview,0);window.scrollTo({top:0,behavior:'smooth'});}
  async function hydrateNewAnalysisResults(){const secondary=window.PropertyThesisSecondaryEngine;if(!secondary?.request)throw new Error('Advanced analysis service is unavailable.');const secondaryResult=await secondary.request({refresh:false});if(!secondaryResult?.offer)throw new Error(secondary.status?.().lastError||'Advanced analysis could not be completed.');const hydration=window.PropertyThesisResultsHydration;if(hydration?.hydrate)await hydration.hydrate({force:true});else{try{window.PropertyThesisSecondaryServerUI?.apply?.();}catch(_e){}try{window.PropertyThesisDecisionCenter?.apply?.();}catch(_e){}try{window.PropertyThesisDecisionCenterStability?.refresh?.();}catch(_e){}}try{window.PropertyThesisDecisionCenterStability?.refresh?.();}catch(_e){}try{window.PropertyThesisDecisionCenter?.apply?.();}catch(_e){}return true;}
  function setCalculating(on){calculating=!!on;const btn=document.getElementById('gwNext');if(!btn)return;if(on){btn.dataset.ptPreviousText=btn.textContent||'Calculate & Review Results →';btn.textContent='Calculating…';btn.disabled=true;btn.setAttribute('aria-busy','true');}else{btn.disabled=false;btn.removeAttribute('aria-busy');const prior=btn.dataset.ptPreviousText;if(prior)btn.textContent=prior;delete btn.dataset.ptPreviousText;try{window.PropertyThesisGuidedWorkflowRefinement?.apply?.();}catch(_e){}}}
  async function run(){if(calculating)return;syncVisible();const i=currentStep();if(i<6){if(!valid(i))return;try{window.GuidedAnalysisSetup?.go?.(i+1);}catch(err){}setTimeout(()=>{try{window.GuidedAssumptionGuidance?.apply?.();window.GuidedPage1Cleanup?.apply?.();bindButton();ensureBox();clearValidation();}catch(e){}},0);return;}clearValidation();setCalculating(true);try{await safeRecalculate();try{if(typeof setStatus==='function')setStatus('Analysis updated — preparing decision results');}catch(e){}activateDashboard();await hydrateNewAnalysisResults();try{if(typeof setStatus==='function')setStatus('Analysis updated — review the results');}catch(e){}}catch(err){const box=ensureBox();if(box){box.innerHTML=`<b>Unable to calculate the analysis.</b><div>${String(err.message||err)}</div>`;box.classList.add('show');}try{if(typeof setStatus==='function')setStatus('Unable to calculate: '+err.message);}catch(_e){}}finally{setCalculating(false);}}
  function capture(e){if(!e.target?.closest?.('#gwNext'))return;e.preventDefault();e.stopPropagation();e.stopImmediatePropagation?.();run();}
  function bindButton(){const btn=document.getElementById('gwNext');if(!btn)return false;btn.onclick=e=>{e.preventDefault();e.stopPropagation();run();};ensureBox();return true;}
  function clearFieldError(e){const input=e.target?.closest?.('#gwBody [data-src]');if(!input)return;input.removeAttribute('aria-invalid');input.closest('.gw-field')?.classList.remove('gw-missing');if(!document.querySelector('#gwBody .gw-field.gw-missing')){const box=document.getElementById('gwValidation');if(box)box.classList.remove('show');}}
  function start(){window.addEventListener('click',capture,true);document.addEventListener('input',clearFieldError,true);let tries=0;const timer=setInterval(()=>{if(bindButton())clearInterval(timer);if(++tries>60)clearInterval(timer)},120);const host=document.getElementById('guidedSetup');if(host){new MutationObserver(()=>bindButton()).observe(host,{childList:true,subtree:true});}}
  window.GuidedContinueController={run,bindButton,validate:valid,recalculate:safeRecalculate,refreshReview,hydrateResults:hydrateNewAnalysisResults};
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
})();