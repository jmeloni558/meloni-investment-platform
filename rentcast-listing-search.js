'use strict';
(()=>{
  const VERSION=20,IMPORT_KEY='ptPendingListingImportV1',SEARCH_KEY='ptListingSearchStateV1';
  if((window.__ptRentCastListingSearchV||0)>=VERSION)return;
  window.__ptRentCastListingSearchV=VERSION;
  let panel=null,results=[],offset=0,activeListing=null;const featureCache=new Map(),rentCache=new Map();
  const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const money=v=>Number.isFinite(Number(v))?Number(v).toLocaleString('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}):'—';
  const value=v=>v===null||v===undefined||v===''?'Not reported':String(v);
  const signedIn=()=>typeof cloudUser!=='undefined'&&!!cloudUser;
  function openAccount(mode='signup',message=''){
    if(window.PropertyThesisAuth?.open){window.PropertyThesisAuth.open(mode,message);return;}
    if(typeof showAuth==='function'){showAuth();return;}
    document.getElementById('authModal')?.classList.remove('hidden');
  }
  const streetViewUrl=l=>{const key=window.PROPERTYTHESIS_CONFIG?.googlePlacesKey||'';const location=Number.isFinite(Number(l.latitude))&&Number.isFinite(Number(l.longitude))?`${l.latitude},${l.longitude}`:(l.formattedAddress||'');if(!key||!location)return'';const q=new URLSearchParams({size:'640x360',location,key,source:'outdoor',fov:'80',pitch:'0',return_error_code:'true'});return`https://maps.googleapis.com/maps/api/streetview?${q}`;};
  const streetView=l=>{const src=streetViewUrl(l),address=l.formattedAddress||'the property';return`<div class="pt-listing-streetview${src?'':' is-unavailable'}">${src?`<img data-streetview data-src="${esc(src)}" alt="Google Street View of ${esc(address)}">`:''}<span>Street View unavailable</span><em>Google Street View</em></div>`;};
  function bindStreetViews(host){const images=[...host.querySelectorAll('[data-streetview]')];const load=img=>{if(!img.src&&img.dataset.src)img.src=img.dataset.src;img.addEventListener('error',()=>img.closest('.pt-listing-streetview')?.classList.add('is-unavailable'),{once:true});};if(!('IntersectionObserver'in window)){images.forEach(load);return;}const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){load(entry.target);observer.unobserve(entry.target);}}),{rootMargin:'180px'});images.forEach(img=>observer.observe(img));}
  function addRangeFields(host){
    const beforeSquareFeet=host.querySelector('[name="squareFootageMin"]')?.closest('.pt-listings-field');
    beforeSquareFeet?.insertAdjacentHTML('beforebegin','<div class="pt-listings-field"><label>Maximum bedrooms</label><input name="bedroomsMax" type="number" min="0"></div><div class="pt-listings-field"><label>Maximum bathrooms</label><input name="bathroomsMax" type="number" min="0" step=".5"></div>');
    const beforeYear=host.querySelector('[name="yearBuiltMin"]')?.closest('.pt-listings-field');
    beforeYear?.insertAdjacentHTML('beforebegin','<div class="pt-listings-field"><label>Minimum lot size (sq. ft.)</label><input name="lotSizeMin" inputmode="numeric"></div><div class="pt-listings-field"><label>Maximum lot size (sq. ft.)</label><input name="lotSizeMax" inputmode="numeric"></div>');
  }
  function organizePropertyFilters(host){
    const groups=[
      ['Purchase price','Set the acquisition range you want to review.',['minPrice','maxPrice']],
      ['Interior','Narrow results by reported rooms and living area.',['bedroomsMin','bedroomsMax','bathroomsMin','bathroomsMax','squareFootageMin','squareFootageMax']],
      ['Site & building','Refine by lot area and construction year.',['lotSizeMin','lotSizeMax','yearBuiltMin','yearBuiltMax']],
      ['Listing activity','Control recency and how results are ordered.',['daysOld','sort']],
    ];
    host.insertAdjacentHTML('afterbegin','<div class="pt-filter-intro"><strong>Refine the opportunity</strong><span>Leave any field blank to keep the search broad.</span></div>');
    groups.forEach(([title,description,names])=>{
      const section=document.createElement('section');section.className='pt-filter-section';
      section.innerHTML=`<header><h3>${title}</h3><p>${description}</p></header><div class="pt-filter-fields"></div>`;
      const fields=section.querySelector('.pt-filter-fields');
      names.forEach(name=>{const field=host.querySelector(`[name="${name}"]`)?.closest('.pt-listings-field');if(field)fields.appendChild(field);});
      host.appendChild(section);
    });
  }

  function ensurePanel(){
    if(panel)return panel;
    const workflow=document.getElementById('stage8Workflow');if(!workflow)return null;
    panel=document.createElement('section');panel.id='ptListingsPanel';panel.className='pt-listings-panel screen-only';
    panel.innerHTML=`<header class="pt-listings-head"><div><div class="pt-listings-eyebrow">NATIONWIDE INVESTMENT DISCOVERY</div><h2>Search Investment Listings</h2><p>Search active listings nationwide. Multifamily and apartment properties are selected initially, and you can include other property types when they fit your investment strategy.</p></div><button class="pt-listings-close" type="button" aria-label="Close listing search">Close</button></header><form class="pt-listings-search"><fieldset class="pt-listings-location"><legend>Location</legend><div class="pt-listings-field pt-listings-address"><label>Starting address</label><input name="address" autocomplete="street-address" placeholder="Enter an address for a radius search"></div><div class="pt-listings-field"><label>Radius</label><select name="radius"><option value="1">1 mile</option><option value="3">3 miles</option><option value="5">5 miles</option><option value="10" selected>10 miles</option><option value="25">25 miles</option><option value="50">50 miles</option><option value="100">100 miles</option></select></div><div class="pt-listings-location-or" aria-hidden="true">OR SEARCH AN AREA</div><div class="pt-listings-field"><label>City</label><input name="city" autocomplete="address-level2"></div><div class="pt-listings-field"><label>State</label><input name="state" maxlength="2" autocomplete="address-level1"></div><div class="pt-listings-field"><label>ZIP code</label><input name="zipCode" inputmode="numeric" maxlength="5" autocomplete="postal-code"></div></fieldset><fieldset><legend>Property type</legend><div class="pt-listings-type-grid"><label class="pt-listings-check"><input type="checkbox" name="multiFamily" checked> 2–4 unit multifamily</label><label class="pt-listings-check"><input type="checkbox" name="apartment" checked> 5+ unit apartment</label><label class="pt-listings-check"><input type="checkbox" name="singleFamily"> Single family</label><label class="pt-listings-check"><input type="checkbox" name="condo"> Condo</label><label class="pt-listings-check"><input type="checkbox" name="townhouse"> Townhouse</label><label class="pt-listings-check"><input type="checkbox" name="manufactured"> Manufactured</label><label class="pt-listings-check"><input type="checkbox" name="land"> Land</label></div></fieldset><fieldset class="pt-listings-filter-grid"><legend>Property filters</legend><div class="pt-listings-field"><label>Minimum price</label><input name="minPrice" inputmode="numeric"></div><div class="pt-listings-field"><label>Maximum price</label><input name="maxPrice" inputmode="numeric"></div><div class="pt-listings-field"><label>Minimum bedrooms</label><input name="bedroomsMin" type="number" min="0"></div><div class="pt-listings-field"><label>Minimum bathrooms</label><input name="bathroomsMin" type="number" min="0" step=".5"></div><div class="pt-listings-field"><label>Minimum square feet</label><input name="squareFootageMin" inputmode="numeric"></div><div class="pt-listings-field"><label>Maximum square feet</label><input name="squareFootageMax" inputmode="numeric"></div><div class="pt-listings-field"><label>Built no earlier than</label><input name="yearBuiltMin" inputmode="numeric" maxlength="4"></div><div class="pt-listings-field"><label>Built no later than</label><input name="yearBuiltMax" inputmode="numeric" maxlength="4"></div><div class="pt-listings-field"><label>Listed within (days)</label><input name="daysOld" type="number" min="1"></div><div class="pt-listings-field"><label>Sort results</label><select name="sort"><option value="newest">Newest listings</option><option value="price-asc">Price: low to high</option><option value="price-desc">Price: high to low</option><option value="units-desc">Most units</option></select></div></fieldset><button class="pt-listings-submit" type="submit">Search Listings</button></form><div class="pt-listings-note"><strong>Cost-conscious search:</strong> Listing results and property features are cached. Price-per-unit uses reported listing data and does not request a separate rent estimate.</div><div class="pt-listings-status" role="status">Enter a starting address and radius, a city and state, or a ZIP code to begin.</div><div class="pt-listings-grid"></div>`;
    const heading=panel.querySelector('.pt-listings-head h2'),intro=panel.querySelector('.pt-listings-head p');
    if(heading)heading.textContent='Find a listing. Form the thesis.';
    if(intro)intro.textContent='Search active listings nationwide, screen their income potential, and carry a property directly into a complete PropertyThesis analysis.';
    panel.querySelector('.pt-listings-head')?.insertAdjacentHTML('afterend','<div class="pt-listings-guest-path" hidden><div><strong>Explore before you create an account</strong><span>Search listings, open a property, test its rent and value, then build your one free analysis. Create or sign in only when you calculate and save the results.</span></div><ol><li><b>1</b> Search</li><li><b>2</b> Screen</li><li><b>3</b> Analyze free</li></ol></div>');
    const propertyFilters=panel.querySelector('.pt-listings-filter-grid');
    propertyFilters.insertAdjacentHTML('beforebegin','<fieldset class="pt-listing-types"><legend>Listing type</legend><div class="pt-listings-type-grid"><label class="pt-listings-check"><input type="checkbox" name="listingStandard" checked> Standard</label><label class="pt-listings-check"><input type="checkbox" name="listingNewConstruction" checked> New construction</label><label class="pt-listings-check"><input type="checkbox" name="listingForeclosure" checked> Foreclosure</label><label class="pt-listings-check"><input type="checkbox" name="listingShortSale" checked> Short sale</label></div></fieldset>');
    addRangeFields(propertyFilters);organizePropertyFilters(propertyFilters);
    panel.querySelector('.pt-listings-note').insertAdjacentHTML('afterend','<div class="pt-listings-usage" hidden><div><strong data-usage-title></strong><span data-usage-copy></span></div><a href="pricing.html">Compare plans</a></div>');
    workflow.insertAdjacentElement('beforebegin',panel);
    panel.querySelector('form').addEventListener('submit',e=>{e.preventDefault();search()});
    panel.querySelector('.pt-listings-close').onclick=close;
    return panel;
  }
  function ensureButton(){
    const actions=document.querySelector('#appNavShell .app-nav-actions');if(!actions)return false;
    let btn=document.getElementById('appNavListings');
    if(!btn){btn=document.createElement('button');btn.type='button';btn.id='appNavListings';btn.className='app-nav-action';btn.textContent='Search Listings';btn.onclick=open;document.getElementById('appNavExisting')?.insertAdjacentElement('afterend',btn);}
    btn.hidden=false;return true;
  }
  function hideApplication(on){document.querySelectorAll('.section').forEach(s=>s.style.display=on?'none':'');const workflow=document.getElementById('stage8Workflow');if(workflow)workflow.style.display=on?'none':'';document.getElementById('appMortgageToolsPanel')?.style.setProperty('display','none');}
  function refreshAccess(){if(!panel)return;const guest=!signedIn(),path=panel.querySelector('.pt-listings-guest-path'),note=panel.querySelector('.pt-listings-note');if(path)path.hidden=!guest;if(note)note.innerHTML=guest?'<strong>Temporary testing access:</strong> Guest listing searches are currently unrestricted while PropertyThesis is being tested. Open, screen and carry any returned listing into your free analysis without an account.':'<strong>Cost-conscious search:</strong> Listing results and property features are cached. Price-per-unit uses reported listing data and avoids unnecessary data calls.';}
  function dedicatedGuestRoute(){return !signedIn()&&new URLSearchParams(location.search).get('listing-search')==='1';}
  function open(){ensurePanel();refreshAccess();hideApplication(true);panel.classList.add('is-open');document.getElementById('appNavListings')?.classList.add('active');window.scrollTo({top:dedicatedGuestRoute()?0:document.getElementById('appNavShell')?.offsetTop||0,behavior:'auto'});}
  function close(){if(dedicatedGuestRoute()){location.href='index.html';return;}panel?.classList.remove('is-open');hideApplication(false);document.getElementById('appNavListings')?.classList.remove('active');}
  function query(){
    const form=panel.querySelector('form');
    const raw=name=>form.elements[name].value.trim().replace(/[$,]/g,'');
    const types=[['singleFamily','Single Family'],['condo','Condo'],['townhouse','Townhouse'],['manufactured','Manufactured'],['multiFamily','Multi-Family'],['apartment','Apartment'],['land','Land']];
    const propertyTypes=types.filter(([name])=>form.elements[name].checked).map(([,type])=>type);
    const listingTypes=[['listingStandard','Standard'],['listingNewConstruction','New Construction'],['listingForeclosure','Foreclosure'],['listingShortSale','Short Sale']].filter(([name])=>form.elements[name].checked).map(([,type])=>type);
    const optional=['minPrice','maxPrice','bedroomsMin','bedroomsMax','bathroomsMin','bathroomsMax','squareFootageMin','squareFootageMax','lotSizeMin','lotSizeMax','yearBuiltMin','yearBuiltMax','daysOld'];
    const filters=Object.fromEntries(optional.map(name=>[name,raw(name)||null]));
    return{address:raw('address'),radius:raw('radius'),city:raw('city'),state:raw('state').toUpperCase(),zipCode:raw('zipCode'),...filters,propertyTypes,listingTypes,sort:raw('sort'),limit:18,offset};
  }
  async function search(){
    offset=0;const q=query(),pairs=[['minPrice','maxPrice','price'],['bedroomsMin','bedroomsMax','bedrooms'],['bathroomsMin','bathroomsMax','bathrooms'],['squareFootageMin','squareFootageMax','square footage'],['lotSizeMin','lotSizeMax','lot size'],['yearBuiltMin','yearBuiltMax','year built']];
    if(!q.address&&!q.zipCode&&!(q.city&&q.state)){setStatus('Enter a starting address, a ZIP code, or both a city and two-letter state code.');return;}if(!q.propertyTypes.length){setStatus('Select at least one property type.');return;}if(!q.listingTypes.length){setStatus('Select at least one listing type.');return;}
    const invalid=pairs.find(([min,max])=>q[min]!==null&&q[max]!==null&&Number(q[min])>Number(q[max]));if(invalid){setStatus(`Minimum ${invalid[2]} cannot be greater than maximum ${invalid[2]}.`);panel.querySelector(`[name="${invalid[0]}"]`)?.focus();return;}
    const submit=panel.querySelector('.pt-listings-submit');submit.disabled=true;submit.textContent='Searching…';setStatus('Searching active listings…');panel.querySelector('.pt-listings-grid').innerHTML='';
    try{refreshAccess();const {sort,...request}=q;const {data,error}=await cloudClient.functions.invoke('rentcast-sale-listings',{body:request});updateUsage(data?.usage,data?.cached);if(error)throw new Error(data?.error||error.message);results=Array.isArray(data?.listings)?data.listings:[];sortResults(sort);render();const total=Number.isFinite(Number(data?.totalCount))?` of ${Number(data.totalCount).toLocaleString()}`:'';const message=results.length?`${results.length}${total} matching active listing${Number(data?.totalCount||results.length)===1?'':'s'} loaded${data?.cached?' from the recent-search cache':''}.`:'No matching listings were found. Try a nearby ZIP code or broader filters.';setStatus(message);offset=results.length;try{localStorage.setItem(SEARCH_KEY,JSON.stringify({query:q,results,status:message,savedAt:Date.now()}));}catch(_e){}}catch(err){setStatus(err?.message||'Listing search failed. Please try again.');}finally{submit.disabled=false;submit.textContent='Search Listings';}
  }
  function sortResults(mode){const n=v=>Number.isFinite(Number(v))?Number(v):0;if(mode==='price-asc')results.sort((a,b)=>n(a.price)-n(b.price));else if(mode==='price-desc')results.sort((a,b)=>n(b.price)-n(a.price));else if(mode==='units-desc')results.sort((a,b)=>n(b.units)-n(a.units));else results.sort((a,b)=>new Date(b.listedDate||0)-new Date(a.listedDate||0));}
  function setStatus(text){const el=panel?.querySelector('.pt-listings-status');if(el)el.textContent=text;}
  function updateUsage(usage,cached=false){const box=panel?.querySelector('.pt-listings-usage');if(!box||!usage)return;if(usage.plan==='guest_testing'){box.hidden=true;return;}const guest=usage.plan==='guest',free=usage.plan==='free',remaining=Number(usage.dailyRemaining)||0;const warn=guest||free&&remaining<=2;if(!warn){box.hidden=true;return;}const link=box.querySelector('a');box.hidden=false;box.querySelector('[data-usage-title]').textContent=remaining>0?`${remaining} ${guest?'guest':'free'} listing search${remaining===1?'':'es'} remaining today`:`${guest?'Guest':'Free'} listing allowance used for today`;box.querySelector('[data-usage-copy]').textContent=cached?'This result came from cache and did not use another call.':guest?'Create a free account when you are ready for more searches or to calculate and save an analysis.':'Upgrade for a larger daily discovery allowance and continued property research.';if(link){link.href=guest?'#create-account':'pricing.html';link.textContent=guest?'Create free account':'Compare plans';link.onclick=guest?event=>{event.preventDefault();openAccount('signup','Create a free account for additional listing searches and to calculate and save your analysis.');}:null;}}
  function facts(l){return `<div><b>${esc(value(l.units))}</b>Units reported</div><div><b>${esc(value(l.squareFootage&&Number(l.squareFootage).toLocaleString()))}</b>Square feet</div><div><b>${esc(value(l.yearBuilt))}</b>Year built</div><div><b>${esc(value(l.daysOnMarket))}</b>Days on market</div>`;}
  function render(){const grid=panel.querySelector('.pt-listings-grid');grid.innerHTML=results.map((l,i)=>`<article class="pt-listing-card">${streetView(l)}<div class="pt-listing-card-top"><span class="pt-listing-type">${esc(l.propertyType)}</span></div><div class="pt-listing-card-body"><div class="pt-listing-price">${money(l.price)}</div><div class="pt-listing-address">${esc(l.formattedAddress||l.addressLine1||'Address unavailable')}</div><div class="pt-listing-facts">${facts(l)}</div><div class="pt-listing-card-actions"><button type="button" data-detail="${i}">Open Listing</button><button type="button" class="primary" data-analyze="${i}">Analyze</button></div></div></article>`).join('');bindStreetViews(grid);grid.querySelectorAll('[data-detail]').forEach(b=>b.onclick=()=>detail(results[Number(b.dataset.detail)]));grid.querySelectorAll('[data-analyze]').forEach(b=>b.onclick=()=>importListing(results[Number(b.dataset.analyze)]));}
  const reportedBoolean=value=>value===true?'Yes':value===false?'No':'Not reported';
  const pct=v=>Number.isFinite(Number(v))?`${(Number(v)*100).toFixed(2)}%`:'—';
  const capRates=[.05,.055,.06,.065,.07],vacancyRate=.10,operatingExpenseRate=.40;
  function rentModel(listing,monthlyRent,source,rangeLow=null,rangeHigh=null){
    const units=Math.max(1,Number(listing.units)||1),multi=listing.propertyType==='Multi-Family'||listing.propertyType==='Apartment';
    const perUnit=Number(monthlyRent)||0,grossMonthly=multi&&units>1?perUnit*units:perUnit;
    return{perUnit,grossMonthly,units,multi,source,rangeLow:Number(rangeLow)||null,rangeHigh:Number(rangeHigh)||null};
  }
  function renderValueView(modal,listing,model,error=''){
    const root=modal.querySelector('[data-value-view]');if(!root)return;
    if(error){root.innerHTML=`<div class="pt-value-empty"><b>Rent estimate unavailable</b><span>${esc(error)} Enter a monthly gross rent below to screen the opportunity.</span><label>Monthly gross rent<input type="number" min="0" step="25" data-value-rent></label></div>`;bindValueInput(root,listing,null);return;}
    if(!model){root.innerHTML='<div class="pt-value-loading"><b>Building the PropertyThesis value view…</b><span>Locating rental evidence and calculating income-supported values.</span></div>';return;}
    const desiredCap=Number(model.desiredCap)||.065,price=Number(listing.price)||0,annualGross=model.grossMonthly*12,effectiveGrossIncome=annualGross*(1-vacancyRate),noi=effectiveGrossIncome*(1-operatingExpenseRate),currentCap=price>0?noi/price:null,desiredValue=noi/desiredCap;modal.dataset.screenRent=String(model.grossMonthly);modal.dataset.screenCap=String(desiredCap*100);
    const sourceNote=model.multi&&model.units>1?`${model.source} per unit × ${model.units} reported units`:model.source;
    const range=model.rangeLow&&model.rangeHigh?`RentCast range ${money(model.rangeLow)}–${money(model.rangeHigh)}${model.multi&&model.units>1?' per unit':''}. `:'';
    root.innerHTML=`<div class="pt-value-head"><div><span>PROPERTYTHESIS VALUE VIEW</span><h4>What does the income support?</h4></div><span class="pt-rent-source">${esc(sourceNote)}</span></div><div class="pt-value-summary"><div><span>Monthly gross rent</span><b>${money(model.grossMonthly)}</b></div><div><span>Estimated NOI</span><b>${money(noi)}</b></div><div class="is-primary"><span>Cap rate at list price</span><b>${pct(currentCap)}</b></div><div class="is-desired"><span>Value at your ${pct(desiredCap)} cap</span><b>${money(desiredValue)}</b></div></div><div class="pt-value-controls"><div class="pt-value-controls-head"><span>TEST YOUR ASSUMPTIONS</span><b>Adjust either input to recalculate the opportunity</b></div><label class="pt-value-control is-rent"><span>Monthly gross rent</span><input type="number" min="0" step="25" value="${Math.round(model.grossMonthly)}" data-value-rent><em>Use total property rent</em></label><label class="pt-value-control is-cap"><span>Desired cap rate</span><div><input type="number" min="1" max="20" step="0.1" value="${(desiredCap*100).toFixed(1)}" data-value-cap><i>%</i></div><em>Your investment benchmark</em></label><p>Press Enter or leave a field to update.</p></div><div class="pt-value-ladder"><div class="pt-value-ladder-head"><b>Cap-rate benchmark</b><b>Income-supported value</b></div>${capRates.map(rate=>`<div class="${rate===.065?'is-benchmark':''}"><span>${(rate*100).toFixed(1)}%${rate===.065?' · benchmark':''}</span><b>${money(noi/rate)}</b></div>`).join('')}</div><div class="pt-value-assumption"><b>Quick-screen assumptions:</b> 10% vacancy and credit loss is deducted from potential gross rent, then operating expenses equal 40% of Effective Gross Income. This matches the default PropertyThesis calculator methodology. ${esc(range)}This is a preliminary income screen, not a substitute for verified leases, expenses, vacancy, reserves, capital costs, or complete underwriting.</div>`;
    bindValueInput(root,listing,model);
  }
  function bindValueInput(root,listing,model){const rentInput=root.querySelector('[data-value-rent]'),capInput=root.querySelector('[data-value-cap]');const apply=()=>{const gross=Number(rentInput?.value),capPct=Number(capInput?.value),rentValid=rentInput?.value!==''&&Number.isFinite(gross)&&gross>0&&gross<=1000000,capValid=capInput?.value!==''&&Number.isFinite(capPct)&&capPct>=1&&capPct<=20;rentInput?.setCustomValidity(rentValid?'':'Enter monthly gross rent between $1 and $1,000,000.');capInput?.setCustomValidity(capValid?'':'Enter a desired cap rate from 1% to 20%.');if(!rentValid){rentInput?.reportValidity();return;}if(!capValid){capInput?.reportValidity();return;}renderValueView(root.closest('.pt-listing-detail'),listing,{...(model||rentModel(listing,gross,'User-entered rent')),grossMonthly:gross,desiredCap:capPct/100,source:gross!==Number(model?.grossMonthly)?'User-entered rent':model?.source||'User-entered rent',multi:gross!==Number(model?.grossMonthly)?false:!!model?.multi});};[rentInput,capInput].forEach(input=>{input?.addEventListener('change',apply);input?.addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();apply();}});});}
  async function loadValueView(listing,modal){
    if(listing.propertyType==='Land'){renderValueView(modal,listing,null,'Land listings require a user-provided income assumption.');return;}
    const prior=listing.recentRentalListing;if(prior?.monthlyRent){renderValueView(modal,listing,rentModel(listing,prior.monthlyRent,`Prior advertised rent${prior.date?` (${prior.date})`:''}`));return;}
    if(!signedIn()){renderValueView(modal,listing,null,'Enter the disclosed or expected rent to test this listing.');return;}
    const key=listing.id||listing.formattedAddress;if(rentCache.has(key)){renderValueView(modal,listing,rentCache.get(key));return;}
    try{const {data,error}=await cloudClient.functions.invoke('rentcast-sale-listings',{body:{action:'rent-estimate',address:listing.formattedAddress,latitude:listing.latitude,longitude:listing.longitude,propertyType:listing.propertyType,bedrooms:listing.bedrooms,bathrooms:listing.bathrooms,squareFootage:listing.squareFootage}});updateUsage(data?.usage,data?.cached);if(error)throw new Error(data?.error||error.message);const estimate=data?.estimate;if(!estimate?.monthlyRent)throw new Error('No market rent estimate was returned.');const model=rentModel(listing,estimate.monthlyRent,'RentCast market rent estimate',estimate.rangeLow,estimate.rangeHigh);rentCache.set(key,model);if(activeListing?.id===listing.id)renderValueView(modal,listing,model);}catch(error){if(activeListing?.id===listing.id)renderValueView(modal,listing,null,error?.message||'No automated rent estimate is available.');}
  }
  function updateFeatureFacts(modal,features,error){
    const garage=features?.garage===true&&features?.garageSpaces?`Yes · ${features.garageSpaces} space${features.garageSpaces===1?'':'s'}`:reportedBoolean(features?.garage);
    const values={garage,garageType:features?.garageType||'Not reported',pool:reportedBoolean(features?.pool),poolType:features?.poolType||'Not reported'};
    modal.querySelectorAll('[data-property-feature]').forEach(el=>{el.textContent=error?'Unavailable':values[el.dataset.propertyFeature]||'Not reported';});
    const status=modal.querySelector('[data-feature-status]');if(status)status.textContent=error?'Additional public-record features could not be loaded.':'Garage and pool information comes from public property records and may vary by county.';
  }
  async function loadFeatureFacts(listing,modal){
    if(!listing.id){updateFeatureFacts(modal,null,true);return;}
    if(featureCache.has(listing.id)){updateFeatureFacts(modal,featureCache.get(listing.id),false);return;}
    try{const {data,error}=await cloudClient.functions.invoke('rentcast-sale-listings',{body:{action:'property-features',propertyId:listing.id}});if(error)throw error;const features=data?.features||{};featureCache.set(listing.id,features);if(activeListing?.id===listing.id)updateFeatureFacts(modal,features,false);}catch(_error){if(activeListing?.id===listing.id)updateFeatureFacts(modal,null,true);}
  }
  function detail(l){
    activeListing=l;let modal=document.getElementById('ptListingDetail');if(!modal){modal=document.createElement('div');modal.id='ptListingDetail';modal.className='pt-listing-detail';document.body.appendChild(modal);modal.addEventListener('click',e=>{if(e.target===modal)modal.classList.remove('is-open')});}
    modal.innerHTML=`<article class="pt-listing-dialog" role="dialog" aria-modal="true" aria-labelledby="ptListingTitle">${streetView(l)}<header class="pt-listing-dialog-head"><div><span class="pt-listing-type">${esc(l.propertyType)}</span><h3 id="ptListingTitle">${esc(l.formattedAddress||'Investment property')}</h3></div><div class="pt-listing-dialog-controls"><button class="pt-listings-expand" type="button" aria-pressed="false">Expand</button><button class="pt-listings-close" type="button">Close</button></div></header><div class="pt-listing-dialog-body"><div class="pt-listing-detail-price">${money(l.price)}</div><div class="pt-listing-detail-grid">${facts(l)}<div><b>${esc(value(l.bedrooms))}</b>Bedrooms reported</div><div><b>${esc(value(l.bathrooms))}</b>Bathrooms reported</div><div><b>${esc(value(l.lotSize&&Number(l.lotSize).toLocaleString()))}</b>Lot square feet</div><div><b>${esc(value(l.listingType))}</b>Listing type</div><div><b>${esc(value(l.mlsNumber))}</b>Listing number</div></div><section class="pt-listing-value-view" data-value-view></section><section class="pt-listing-features"><header><h4>Property features</h4><span data-feature-status>Loading garage and pool information…</span></header><div class="pt-listing-feature-grid"><div><b data-property-feature="garage">Loading…</b>Garage</div><div><b data-property-feature="garageType">Loading…</b>Garage type</div><div><b data-property-feature="pool">Loading…</b>Pool</div><div><b data-property-feature="poolType">Loading…</b>Pool type</div></div></section><section class="pt-listing-analyze"><header><span>GO BEYOND THE QUICK SCREEN</span><h4>Build the complete investment case</h4><p>Bring this listing into the guided PropertyThesis analysis with its address, list price and property details already started.</p></header><div class="pt-listing-analyze-grid"><div><b>Control the assumptions</b><span>Adjust rent, vacancy, operating expenses, financing, taxes, holding period, growth, exit terms and valuation targets.</span></div><div><b>Understand the decision</b><span>Review cash flow, cap rate, DSCR, IRR, NPV, sensitivity, supported value and offer guidance.</span></div><div><b>Save and present the work</b><span>Save the property and analysis, generate a multiyear Excel pro forma, and create a professional property report.</span></div></div><button class="pt-listings-action" type="button" data-import>Analyze This Property <span aria-hidden="true">→</span></button><small>Your imported listing remains editable. Verify listing information and replace estimates with known property data when available.</small></section><div class="pt-listing-attribution">Listing information provided by RentCast${l.mlsName?` from ${esc(l.mlsName)}`:''}. Exterior imagery is supplied separately by Google Street View and is not listing photography. Verify all information independently before making an investment decision.</div></div></article>`;
    bindStreetViews(modal);modal.querySelector('.pt-listings-close').onclick=()=>modal.classList.remove('is-open');const expand=modal.querySelector('.pt-listings-expand'),dialog=modal.querySelector('.pt-listing-dialog');expand.onclick=()=>{const expanded=dialog.classList.toggle('is-expanded');expand.textContent=expanded?'Restore':'Expand';expand.setAttribute('aria-pressed',String(expanded));};const importButton=modal.querySelector('[data-import]');if(!signedIn()){importButton.innerHTML='Start My Free Analysis <span aria-hidden="true">→</span>';modal.querySelector('[data-value-view]')?.insertAdjacentHTML('beforebegin','<aside class="pt-listing-account-gate"><div><strong>Unlock additional property intelligence with a free account</strong><span>Automated market-rent estimates and additional public-record features are available after account creation. You can still enter rent manually, test value and begin your free analysis now.</span></div><div><button type="button" data-account-create>Create Free Account</button><button type="button" class="secondary" data-account-signin>Sign In</button></div></aside>');modal.querySelector('[data-account-create]')?.addEventListener('click',()=>openAccount('signup','Create a free account to unlock automated rent estimates and additional property details.'));modal.querySelector('[data-account-signin]')?.addEventListener('click',()=>openAccount('signin','Sign in to unlock automated rent estimates and additional property details.'));}importButton.onclick=()=>importListing(activeListing);modal.classList.add('is-open');renderValueView(modal,l,null);if(signedIn())loadFeatureFacts(l,modal);else{updateFeatureFacts(modal,null,true);const featureStatus=modal.querySelector('[data-feature-status]');if(featureStatus)featureStatus.textContent='Free account required — create an account or sign in to load garage, pool and other public-record details.';}loadValueView(l,modal);
  }
  function importListing(l){
    const minimumUnits=l.propertyType==='Apartment'?5:l.propertyType==='Multi-Family'?2:1;
    const screenRent=Number(document.getElementById('ptListingDetail')?.dataset.screenRent)||0;
    const imported={name:l.formattedAddress||l.addressLine1||'Imported Listing',address:l.formattedAddress||l.addressLine1||'',price:Number(l.price)||0,rent:screenRent,units:Number(l.units)||minimumUnits,propertyType:l.propertyType||'',listing:{...l,source:'RentCast',importedAt:new Date().toISOString()}};
    localStorage.setItem(IMPORT_KEY,JSON.stringify(imported));
    if(!signedIn()){
      const params=new URLSearchParams({'free-analysis':'1','starter-address':imported.address,'starter-price':String(imported.price||''),'starter-rent':String(imported.rent||''),'starter-units':String(imported.units||1),cb:String(Date.now())});
      location.href='index.html?'+params.toString();
      return;
    }
    try{state={...defaults,name:imported.name,address:imported.address,price:imported.price,rent:imported.rent,units:imported.units,marketRentSupport:{inputs:{propertyType:imported.propertyType,bedrooms:l.bedrooms??'',bathrooms:l.bathrooms??'',squareFootage:l.squareFootage??''}},sourceListing:imported.listing};renderFields();render();}catch(_e){}
    document.getElementById('ptListingDetail')?.classList.remove('is-open');close();
    try{window.AppNavigationToolbar?.go?.('assumptions');window.GuidedAnalysisSetup?.go?.(1);}catch(_e){}
    try{if(typeof setStatus==='function')setStatus(`Listing imported. Enter the expected monthly rent and verify the ${Number(l.units)?'reported':'minimum assumed'} unit count before calculating.`);}catch(_e){}
    window.scrollTo({top:document.getElementById('guidedSetup')?.offsetTop||document.getElementById('appNavShell')?.offsetTop||0,behavior:'smooth'});
  }
  function requested(){
    const params=new URLSearchParams(location.search);if(params.get('app-action')!=='search-listings'&&params.get('listing-search')!=='1')return;
    open();
  }
  function restoreSearch(){
    if(new URLSearchParams(location.search).get('listing-search')!=='1')return;
    try{
      const saved=JSON.parse(localStorage.getItem(SEARCH_KEY)||'null');if(!saved?.query||!Array.isArray(saved.results)||Date.now()-Number(saved.savedAt)>3600000)return;
      const form=panel.querySelector('form'),q=saved.query;
      Object.entries(q).forEach(([name,val])=>{const control=form.elements[name];if(!control||Array.isArray(val)||val===null||name==='sort')return;control.value=val;});
      const typeMap={'Single Family':'singleFamily','Condo':'condo','Townhouse':'townhouse','Manufactured':'manufactured','Multi-Family':'multiFamily','Apartment':'apartment','Land':'land'};
      Object.values(typeMap).forEach(name=>form.elements[name].checked=false);(q.propertyTypes||[]).forEach(type=>{if(typeMap[type])form.elements[typeMap[type]].checked=true;});
      const listingMap={'Standard':'listingStandard','New Construction':'listingNewConstruction','Foreclosure':'listingForeclosure','Short Sale':'listingShortSale'};
      Object.values(listingMap).forEach(name=>form.elements[name].checked=false);(q.listingTypes||[]).forEach(type=>{if(listingMap[type])form.elements[listingMap[type]].checked=true;});
      form.elements.sort.value=q.sort||'newest';results=saved.results;offset=results.length;render();setStatus(saved.status||`${results.length} matching listings restored.`);
    }catch(_e){}
  }
  function start(){ensurePanel();restoreSearch();let tries=0;const timer=setInterval(()=>{ensureButton();if(++tries>40)clearInterval(timer)},180);document.addEventListener('click',e=>{if(e.target.closest('#appNavShell .app-nav-action:not(#appNavListings)'))close()});document.addEventListener('keydown',e=>{if(e.key==='Escape')document.getElementById('ptListingDetail')?.classList.remove('is-open')});try{cloudClient?.auth?.onAuthStateChange?.(()=>setTimeout(ensureButton,120));}catch(_e){}setTimeout(requested,900);setTimeout(restoreSearch,1100);setTimeout(restoreSearch,1700);}
  window.PropertyThesisListingSearch={open,close,search,importListing};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
})();
