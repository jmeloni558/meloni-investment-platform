'use strict';(()=>{const input=document.getElementById('glossarySearch'),count=document.getElementById('glossaryCount'),cards=[...document.querySelectorAll('.term-grid article')],sections=[...document.querySelectorAll('.term-section')];if(!input)return;function filter(){const q=input.value.trim().toLowerCase();let visible=0;cards.forEach(card=>{const match=!q||card.textContent.toLowerCase().includes(q);card.hidden=!match;if(match)visible++});sections.forEach(section=>section.classList.toggle('no-results',!section.querySelector('.term-grid article:not([hidden])')));count.textContent=q?`${visible} term${visible===1?'':'s'} found`:`${cards.length} terms explained`}input.addEventListener('input',filter);filter()})();
